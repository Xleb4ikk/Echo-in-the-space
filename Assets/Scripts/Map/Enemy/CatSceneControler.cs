using UnityEngine;

public class CatSceneControler : MonoBehaviour
{
    [SerializeField] private Animator MonsterAnimation;
    [SerializeField] private Animator CameraAnimation;


    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
